<h2><%= @category.name %></h2>

<ul>
<%= for product <- @category.products do %>
    <li><%= link product.name, to: product_path(@conn, :show, product) %> $<%= product.price %></li>
<% end %>
</ul>

<%= link "Back", to: category_path(@conn, :index) %>
